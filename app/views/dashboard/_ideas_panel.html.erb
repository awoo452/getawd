<div class="dashboard-panel">
  <div>
    <h3>💡 Ideas</h3>
    <div class="status-legend">
      <div class="legend-item"><div class="color-box green"></div> Related Task Completed Today</div>
      <div class="legend-item"><div class="color-box yellow"></div> 1–2 Days Ago</div>
      <div class="legend-item"><div class="color-box red"></div> 3–6 Days Ago</div>
      <div class="legend-item"><div class="color-box black"></div> 7+ Days</div>
      <div class="legend-item"><div class="color-box gray"></div> Inactive</div>
    </div>
  </div>
  <div class="ideas-indicator">
    <% ideas.each do |entry| %>
      <div class="idea-box <%= entry[:color] %>">
        <%= link_to entry[:emoji],
          idea_path(entry[:id]),
          class: "idea-link",
          title: "#{entry[:goals_count]} goal(s), #{entry[:tasks_count]} task(s)" %>

        <div class="idea-label"><%= entry[:title] %></div>

        <div class="idea-stats">
          <div class="completed">✅ <%= entry[:completed_time] %>m</div>
          <div class="upcoming">⏳ <%= entry[:upcoming_time] %>m</div>
          <div class="partial">➖ <%= entry[:partial_time] %>m</div>
        </div>
      </div>
    <% end %>
  </div>

  <div class="time-legend">
    <div class="legend-item">
      ✅ Completed — Total actual minutes logged on finished tasks
    </div>
    <div class="legend-item">
      ⏳ Upcoming — Remaining minutes (estimated − actual) for tasks not yet completed
    </div>
    <div class="legend-item">
      ➖ Partial — Minutes already logged on tasks currently in progress
    </div>
  </div>

  <div class="dashboard-nav">
    <%= link_to "➕ New Task", new_task_path, class: "new-task-btn" %>
    <%= link_to "➕ New Goal", new_goal_path, class: "new-goal-btn" %>
  </div>
</div>
