<ul>
  <% projects.each do |project| %>
    <li>
      <h3><%= project.title %></h3>
      <p><%= project.description %></p>

      <% if project.image.present? %>
        <%= image_tag project_image_url(project.image) %>
      <% end %>

      <div>
        <% if project.url.present? %>
          <a href="<%= project.url %>">View Project</a>
        <% end %>
      </div>
    </li>
  <% end %>
</ul>
