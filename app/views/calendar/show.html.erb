<% goal_icons = defined?(GOAL_ICONS) ? GOAL_ICONS : {} %>
<%= render "daily_tasks", tasks: @tasks, goal_icons: goal_icons %>
<hr class="calendar-divider">
<%= render "monthly_tasks", tasks: @tasks, goal_icons: goal_icons %>
