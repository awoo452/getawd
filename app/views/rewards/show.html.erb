<h1><%= @reward.name %></h1>

<p><%= @reward.description %></p>

<% if @reward.reward_payload.present? && @reward.reward_payload["game_title"].present? %>
  <h2>🎮 Your game</h2>
  <p><strong><%= @reward.reward_payload["game_title"] %></strong></p>

  <p>
    Redeemed at:
    <%= l(Time.zone.parse(@reward.reward_payload["redeemed_at"]), format: :short) rescue "Unknown" %>
  </p>

<% else %>
  <p>No game has been assigned yet.</p>
<% end %>

<%= link_to "Back to Rewards", rewards_path %>