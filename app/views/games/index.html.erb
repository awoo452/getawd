<%= render "shared/logged_in_heading", title: "Gaming Highlights" %>
<section class="games">
  <%= render "game_list", games: @games %>
  <%= render "shared/pagination",
        page: @games_page,
        total_pages: @games_total_pages,
        path_helper: :games_path %>
</section>
